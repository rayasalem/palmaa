# سبب فشل تسجيل الدخول: مفتاح Supabase الخاطئ

الباكند **يجب** أن يستخدم مفتاح **service_role** (السري) وليس مفتاح **anon** (العلني).

---

## ماذا تفعل الآن

1. ادخل **Supabase** → مشروعك → **Settings** (الإعدادات) → **API**.
2. ستجد مفتاحين:
   - **anon / public** (علني) — يُستخدم في الفرونت فقط، ولا يصلح للباكند.
   - **service_role** (سري) — هذا هو المطلوب للباكند.
3. انسخ قيمة **service_role** (اضغط Reveal ثم Copy).
4. ضعها في الباكند فقط:
   - **محلياً:** في ملف `server/.env` كمتغير:
     - `SUPABASE_SERVICE_KEY=اللصق_هنا`
   - **على Render:** في مشروع الباكند → **Environment** → أضف أو عدّل:
     - الاسم: `SUPABASE_SERVICE_KEY` أو `SUPABASE_SERVICE_ROLE_KEY`
     - القيمة: مفتاح **service_role** (ليس anon).
5. أعد تشغيل الباكند (أو انتظر إعادة النشر على Render).

---

## ملاحظة عن ملف .env في جذر المشروع

إذا في الجذر عندك `.env` وفيه سطر مثل:

`SUPABASE_SERVICE_KEY=eyJ...` (مفتاح anon)

فهذا يسبب المشكلة إذا كان الباكند يقرأ من هذا الملف. غيّر القيمة إلى مفتاح **service_role** في مكان تشغيل الباكند فقط (مثلاً `server/.env` أو متغيرات Render)، ولا تضع مفتاح service_role في الفرونت أو في ملفات تُرفع على Git.

---

بعد استبدال المفتاح بمفتاح **service_role** في الباكند، جرّب تسجيل الدخول مرة ثانية.
