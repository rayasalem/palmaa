/**
 * Arabic Bank payment module – Express router.
 * Mount at /api/payment (alongside existing create/callback) or /api/payment/arabic-bank.
 */

import paymentRoutes from './routes/paymentRoutes.js';

export default paymentRoutes;
