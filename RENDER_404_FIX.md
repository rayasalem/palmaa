# ليش 404 و 401؟ وكيف تصلح على Render

## السبب

**404 على `/api/auth/me`** = الطلبات واصلة لسيرفر **ما فيه** مسارات الـ API (يعني غالباً سيرفر الفرونت فقط).

يعني: الرابط اللي فاتح منه الموقع (مثلاً `palmaa.onrender.com`) **مش** الباكند اللي يشغّل `server/server.js`.

---

## تحقق بسرعة

افتح في المتصفح:

**https://palmaa.onrender.com/api/auth/ping**

- لو ظهر: `{"ok":true,"api":"auth",...}` → هذا الرابط **هو** الباكند، والمشكلة من غيره (مثلاً 401 من الداتا أو المفتاح).
- لو ظهر **404** → هذا الرابط **ليس** الباكند، والفرونت يبعت طلبات الـ API لنفس الرابط فيحصل 404 و 401.

---

## الحل: عندك خياران على Render

### الخيار أ: خدمة واحدة (باكند + فرونت مع بعض)

يعني: نفس الخدمة تشغّل **Node (الباكند)** وتخدم ملفات الفرونت من `server/public`.

1. في Render اللي اسمه أو رابطته `palmaa.onrender.com`:
   - **Root Directory:** `server`
   - **Build Command:** `npm install`
   - **Start Command:** `node server.js`
2. الفرونت لازم يكون مبنّي ومحطوط داخل الباكند:
   - على جهازك من **جذر** المشروع شغّل:
     - `npm run build:for-render`
     - (هذا يبني الفرونت ثم ينسخ `dist` إلى `server/public` تلقائياً)
   - أو يدوياً: `npm run build` ثم انسخ محتويات **`dist`** إلى **`server/public`**.
   - ارفع التعديلات (commit + push) عشان Render يعيد النشر.
3. بعد النشر، `palmaa.onrender.com` يعطي الموقع والـ API مع بعض، و **VITE_API_URL** يبقى نفس الرابط:
   - `VITE_API_URL=https://palmaa.onrender.com`

بهذا الشكل الطلبات ما تروح لسيرفر ثاني، و 404 على `/api/auth/me` تختفي.

---

### الخيار ب: خدمتين (فرونت على رابط، باكند على رابط ثاني)

يعني: خدمة **Static Site** (أو استضافة ثانية) للفرونت، وخدمة **Web Service** للباكند فقط.

1. **خدمة الباكند (Web Service):**
   - **Root Directory:** `server`
   - **Start Command:** `node server.js`
   - بعد النشر انسخ **رابط** الخدمة (مثلاً `https://palma-api.onrender.com` أو أي رابط يعطيك Render).

2. **الفرونت:**
   - لازم يُبنى مع **رابط الباكند** (مو رابط الفرونت):
     - `VITE_API_URL=https://رابط-الباكند-من-Render`
     - مثال: `VITE_API_URL=https://palma-api.onrender.com`
   - ثم `npm run build` ورفع محتويات **`dist`** (على cPanel أو Static Site على Render).

3. **مهم:** ما تضع **رابط الفرونت** في `VITE_API_URL` (مثلاً لو الفرونت على `palmaa.onrender.com` لا تضع هذا الرابط في `VITE_API_URL`).  
   **VITE_API_URL** = رابط خدمة الـ **Web Service** اللي فيها الباكند فقط.

لو الفرونت مبنّي مع رابط الباكند الصحيح، طلبات `/api/auth/me` و `/api/auth/login` تروح للباكند و 404 تختفي؛ و 401 نصلحها بمفتاح Supabase (service_role) كما في الملفات الثانية.

---

## ملخص

| المشكلة | السبب المحتمل | الحل |
|--------|----------------|-----|
| 404 على `/api/auth/me` | الرابط الحالي يشغّل فرونت فقط (ما فيه API) | إما تشغّل الباكند من مجلد `server` وتبعث الفرونت من `server/public` (خيار أ)، أو تبني الفرونت مع `VITE_API_URL` = رابط خدمة الباكند (خيار ب). |
| 401 على `/api/auth/login` | بعد حل 404 إذا لسه 401: مفتاح Supabase أو بيانات دخول | استخدم مفتاح **service_role** في الباكند (في Render Environment و/أو `server/.env`) وراجع البريد وكلمة المرور. |

بعد تطبيق الخيار المناسب (أ أو ب) وضبط المفتاح، جرّب تسجيل الدخول مرة ثانية.
