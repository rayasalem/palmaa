# خطوات حتى يشتغل تسجيل الدخول

## 1) على Render (الباكند)

- ادخل **Render** → مشروع الباكند (palmaa.onrender.com).
- **Environment** → تأكد من:
  - **SUPABASE_SERVICE_KEY** = مفتاح **service_role** من Supabase (Settings → API → service_role)، **ليس** مفتاح anon.
  - **SUPABASE_URL** = رابط مشروعك (مثل https://xxxxx.supabase.co).
- احفظ ثم **Manual Deploy** (أو انتظر إعادة النشر) عشان التعديلات والـ env يطبّقون.

---

## 2) في Supabase (قاعدة البيانات)

- ادخل **Supabase** → **SQL Editor**.
- شغّل جزء المستخدمين التجريبيين من **supabase/setup.sql** (القسم اللي فيه INSERT INTO users مع admin@palma.demo)، أو شغّل الملف كامل إذا ما عملته من قبل.
- تأكد من **Table Editor** → جدول **users** أن فيه مستخدم مثل **admin@palma.demo** وعمود **password** غير فاضي (يبدأ بـ $2a$ أو $2b$).

---

## 3) تأكد أن آخر كود الباكند مرفوع

- التعديلات اللي نعملها (مثل إصلاح مستخدم التجربة) لازم تكون مرفوعة على Git وتم نشرها على Render.
- من المشروع على جهازك:
  - `git add .`
  - `git commit -m "auth fixes"`
  - `git push`
- بعدها Render يعيد النشر تلقائي (أو اعمل **Manual Deploy**).

---

## 4) جرّب الدخول من الموقع

- افتح الموقع من **https://palma.ps** (أو من أي رابط الفرونت).
- اضغط تسجيل الدخول.
- أدخل:
  - **البريد:** `admin@palma.demo`
  - **كلمة المرور:** `Admin@123456` (بحرف A كبير و @ وأرقام 123456).
- اضغط دخول.

إذا دخلت → معناها الدخول يشتغل. بعدها أي مستخدم مسجّل يمكنه الدخول بنفس طريقته (بريده وكلمة المرور).

---

## 5) إذا لسه 401

- ادخل **Render** → **Logs** لمشروع الباكند.
- جرّب الدخول مرة من الموقع، ثم ابحث في الـ Logs عن سطر يبدأ بـ **`[authService] login:`**.
- انسخ السطر كامل أو لخّصه:
  - `no user found` → البريد غلط أو المستخدم غير موجود في الجدول (راجع الخطوة 2).
  - `hasPassword: false` أو `passwordLen: 0` → مفتاح Supabase غلط (راجع الخطوة 1).
  - `bcrypt compare failed` → كلمة المرور غلط، أو جرّب نسيت كلمة المرور من الموقع وضبط كلمة جديدة ثم ادخل بها.

---

## ملخص سريع

| الخطوة | أين | ماذا تفعل |
|--------|-----|-----------|
| 1 | Render → Environment | SUPABASE_SERVICE_KEY = مفتاح **service_role** |
| 2 | Supabase → SQL Editor | تشغيل setup.sql (أو جزء المستخدمين) |
| 3 | Git + Render | push ثم انتظار النشر أو Manual Deploy |
| 4 | الموقع (palma.ps) | دخول بـ admin@palma.demo / Admin@123456 |
| 5 | Render → Logs | إذا 401 اقرأ سطر `[authService] login:` |

بعد تطبيق الخطوات 1–4 يفترض أن يدخل المستخدم بدون مشكلة.
