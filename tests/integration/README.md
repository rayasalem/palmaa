# Integration tests

Place integration tests here. They verify Controller → Service → Database flow.

**Run:** `npm run test:integration`

Example: create an order via controller, assert it appears in DB (or mocked DB).
