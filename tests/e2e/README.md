# E2E tests (Cypress)

E2E tests are run with Cypress. See project root:

- `npm run cy:open` – open Cypress UI
- `npm run test:e2e` – run Cypress headless

Add scenarios here or in `cypress/e2e/` for: create order, merchant accept order, change status, track order.
